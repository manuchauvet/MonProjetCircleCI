
machine:
  python:
    version: 2.7.12
    
test:
  post:
    - python project.py
    - cat /tmp/monfichier.txt | wc -l
    
deployment:
  automerge:
    branch: v4
    commands:
      - git checkout master
      - git merge master v4
      - git push origin master
